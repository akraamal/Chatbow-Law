# ─────────────────────────────────────────────────────────────────────────────
# ADLI Morocco — chatbot RAG juridique marocain
# ─────────────────────────────────────────────────────────────────────────────

# --- Web (chatbot + analyseur) ---
flask>=3.0.0             # Application web (routes, templates, SSE)
python-dotenv>=1.0.0     # Chargement de GROQ_API_KEY depuis .env
groq>=0.9.0              # API LLM (qwen/qwen3.6-27b par défaut)

# --- Étape 1 : Ingestion ---
pymupdf>=1.24.0           # Extraction de texte depuis les PDF (fitz)
paddleocr>=2.10.0         # OCR chinois-latin-arabe (ocr_extractor_paddle.py) — 2.10.x vérifiée
pytesseract>=0.3.10       # OCR (nécessite le binaire Tesseract installé séparément)
pillow>=10.0.0            # Manipulation d'images pour l'OCR
fasttext-wheel>=0.9.2     # Détection de langue robuste (modèle lid.176.bin à télécharger séparément)
pdfplumber>=0.11.0        # Détection et extraction des tableaux (table_extractor.py)

# Binaire Tesseract requis (hors pip) :
#   apt-get install tesseract-ocr tesseract-ocr-fra tesseract-ocr-ara
# Vérifier : tesseract --list-langs

# --- Étape 3 : Extraction NLP ---
spacy>=3.7.0                # Pipeline blank (fr/ar) + EntityRuler (ministères) + spans regex (dahir/loi/décret/arrêté/BO)

# --- Étape 4 : NER statistique (AR) ---
camel-tools==1.5.7        # Requis par src/extraction/ner_statistical_ar.py (NERecognizer) — épinglé : 1.6+ casse l'API NERecognizer

# --- Étape 4 : NER statistique (FR) ---
# Modèle spaCy entraîné requis par src/extraction/ner_statistical.py :
#   python -m spacy download fr_core_news_md

# --- Étape 6 : Recherche sémantique ---
sentence-transformers>=3.0.0   # Embeddings multilingues (intfloat/multilingual-e5-base par défaut)
faiss-cpu>=1.8.0               # Index vectoriel (IndexFlatIP, recherche exacte)

# --- Scripts / data wrangling ---
numpy>=1.26.0             # Scripts de pipeline et d'audit
pandas>=2.0.0             # build_training_dataset, classification
